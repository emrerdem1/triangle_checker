import { InputNumber } from 'antd';
import styled from '@emotion/styled';

export const TriangleInput = styled(InputNumber)`
  width: ${100 / 3}%;

  .ant-input-number-input {
    text-align: center;
  }
`;
